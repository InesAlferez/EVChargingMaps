import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'EVChargingMaps',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
